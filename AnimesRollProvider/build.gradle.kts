version = 1

cloudstream {
    description = "Plugin para o site AnimesROLL"
    language = "pt"
    authors = listOf("SeuNome")

    // Nome que aparecerá no CloudStream
    pluginName = "AnimesROLL"

    tvTypes = listOf(
        "Anime"
    )

    iconUrl = "https://www.anroll.net/favicon.ico"
}
